#ifndef SEMESTRALWORK_BUILDINGGENERATOR_H
#define SEMESTRALWORK_BUILDINGGENERATOR_H

namespace SW {
    /**
     * Helper class which triggers all default buildings configurations generating.
     */
    class BuildingGenerator {
    public:
        /**
         * Generates all default buildings configurations.
         */
        static void generate();
    };
}

#endif
